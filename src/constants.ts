/** Page size options for paginated lists (e.g. projects, resumes). */
export const PAGE_SIZE_OPTIONS = [5, 10, 25, 50] as const;

/** Default number of items per page when not specified in URL/search. */
export const DEFAULT_PAGE_SIZE = 5;
